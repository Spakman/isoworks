<style>
body {
  text-align: center;
}

ul {
  text-align: left;
  box-sizing: border-box;
  -moz-box-sizing: border-box;
  width: 1125;
  list-style-type: none;
  padding: 0;
  margin: 0 auto;
}

li {
  display: inline-block;
  vertical-align: top;
  box-sizing: border-box;
  -moz-box-sizing: border-box;
  width: 375px;
  padding: 0 1em 1em 0;
  margin: 0;
}

h2 {
  font-size: 1em;
}
</style>
<ul><!-- display: inline-block doesn't play nicely with spaces between elements
  <% @photo_list.photos.each do |photo| %>
 --><li>
      <h2><%= photo.title %></h2>
      <img src="<%= small_photo_url(photo) %>" alt="<%= photo.title %>" />
    </li><!--
  <% end %>
--></ul>
